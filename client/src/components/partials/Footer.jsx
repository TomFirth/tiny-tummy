import React, { PureComponent } from 'React'

class Footer extends PureComponent {
  render () {
    return (
      <footer>
        {/* Twitter */}
        {/* Contact */}
      </footer>
    )
  }
}

export default Footer